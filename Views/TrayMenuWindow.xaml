<Window x:Class="KfuPet.Views.TrayMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KfuPetTrayMenu"
        Width="200"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="True">

    <Window.Resources>
        <!-- 菜单项：背景层铺满整个选项槽位，悬停时淡入；项间留 4px 缝隙 -->
        <Style x:Key="MenuItemStyle" TargetType="Button">
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="HoverBg"
                                    Background="{DynamicResource AppBorderBrush}"
                                    CornerRadius="6"
                                    Opacity="0"/>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    Padding="10,8">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverBg"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="1" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverBg"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Margin 为投影留出空间，避免被窗口边缘裁切 -->
    <Border x:Name="MenuRoot"
            Margin="8"
            Background="{DynamicResource AppSurfaceBrush}"
            BorderBrush="{DynamicResource AppBorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="6"
            Opacity="0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.25" Color="Black"/>
        </Border.Effect>
        <Border.RenderTransform>
            <TranslateTransform Y="6"/>
        </Border.RenderTransform>

        <StackPanel>
            <Button x:Name="CheckUpdateItem" Style="{StaticResource MenuItemStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE72C;"
                               FontFamily="{StaticResource FontFamilyIcon}"
                               FontSize="{StaticResource FontSizeIcon}" Width="26"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource AppTextSecondaryBrush}"/>
                    <TextBlock Text="检查更新"
                               FontFamily="{StaticResource FontFamilyBody}"
                               FontSize="{StaticResource FontSizeBody}"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource AppTextPrimaryBrush}"/>
                </StackPanel>
            </Button>

            <Button x:Name="SettingsItem" Style="{StaticResource MenuItemStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE713;"
                               FontFamily="{StaticResource FontFamilyIcon}"
                               FontSize="{StaticResource FontSizeIcon}" Width="26"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource AppTextSecondaryBrush}"/>
                    <TextBlock Text="设置"
                               FontFamily="{StaticResource FontFamilyBody}"
                               FontSize="{StaticResource FontSizeBody}"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource AppTextPrimaryBrush}"/>
                </StackPanel>
            </Button>

            <Border Height="1"
                    Margin="6,4"
                    Background="{DynamicResource AppBorderBrush}"/>

            <Button x:Name="ExitItem" Style="{StaticResource MenuItemStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE7E8;"
                               FontFamily="{StaticResource FontFamilyIcon}"
                               FontSize="{StaticResource FontSizeIcon}" Width="26"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource AppTextSecondaryBrush}"/>
                    <TextBlock Text="退出"
                               FontFamily="{StaticResource FontFamilyBody}"
                               FontSize="{StaticResource FontSizeBody}"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource AppTextPrimaryBrush}"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Border>
</Window>
