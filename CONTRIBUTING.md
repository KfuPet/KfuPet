# 贡献指南

感谢你有兴趣为 KfuPet 做出贡献 ❤️

本指南包含开发环境搭建、代码规范与贡献流程。提交贡献前请先阅读本文。

<br />

## 🧑‍💻 开发

- 本项目采用 **C#** + **WPF** 开发（基于 .NET 8.0）
- Visual Studio 2022 版本要求：>= 17.8

### 🚀 快速开始

#### 方法一：直接双击解决方案文件（推荐）
1. 在项目文件夹中找到后缀为 **`.sln`** 的文件。
2. 直接双击该文件，系统会自动使用 **Visual Studio** 打开整个解决方案（包含所有子项目）。
3. 如果双击后没有反应，右键该文件，选择 **`打开方式` → `Microsoft Visual Studio`** 即可。

#### 方法二：从 Visual Studio 内部打开
启动 Visual Studio 后，可通过以下任一入口打开项目：

- **开始窗口**  
  点击右侧的 **`打开项目或解决方案`**，然后浏览并选择 `.sln` 或 `.csproj` 文件。

- **菜单栏**  
  依次点击 **`文件`** → **`打开`** → **`项目/解决方案`**（快捷键 `Ctrl + Shift + O`），同样选择 `.sln` 或 `.csproj` 文件。

<br />

## 🐛 提交 Issue

- 提交前请先搜索已有 Issue，避免重复
- 请清晰描述：**复现步骤**、**期望行为**、**实际行为**、**运行环境**（系统版本 / .NET 版本）
- 一个问题对应一个 Issue，方便跟踪

<br />

## 🔀 提交 Pull Request

1. Fork 本仓库，并基于 `main` 创建特性分支
2. 保持 PR **聚焦**：一个 PR 只做一件事，不修改无关文件
3. 不要擅自重命名、删除现有代码或调整项目架构
4. 提交前请确保代码能通过编译，并自查明显的语法/引用错误
5. 在 PR 描述中说明：**改动内容**、**改动原因**、**影响范围**

<br />

## 📝 代码规范

- 类与公共成员使用 `PascalCase`，局部变量使用 `camelCase`，私有字段加 `_` 前缀
- 适当使用 `async/await`，**不要阻塞 UI 线程**
- 公共 API 添加 XML 文档注释，方法保持简短专注
- UI 层与业务逻辑分离（优先 MVVM），不要在 XAML 代码后置中写业务逻辑
- 不硬编码资源路径、配置值、AI Provider、密钥
- 不使用魔法数字，不重复代码
- 不引入不必要的第三方依赖；不擅自升级 NuGet 包或修改 `.csproj`
- 更多架构约定请参阅项目根目录的 [AGENTS.md](AGENTS.md)

<br />

## 📜 许可说明

本项目采用 **双许可模式**（AGPL-3.0 + 商业许可）。

向本项目提交代码（包括 PR、补丁等）即表示你同意：
- 你的贡献以与本项目相同的双许可模式进行授权
- 你拥有所提交代码的合法权利，且该提交不侵犯任何第三方权益

详见 [README](README.md) 的开源许可一节。
